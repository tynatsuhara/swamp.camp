export enum TimeUnit {
    MINUTE = 1500, // millis in an in-game minute
    HOUR = 60 * TimeUnit.MINUTE,
    DAY = 24 * TimeUnit.HOUR,
}

export enum Day {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY,
    SUNDAY,
}
